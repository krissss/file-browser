version: '3.8'

services:
  file-browser:
    build: .
    container_name: file-browser
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - /path/to/your/files:/data:ro
    env_file:
      - .env
